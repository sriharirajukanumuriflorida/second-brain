[pytest]
# Tests live under tests/. asyncio_mode=auto lets async test functions run
# without an explicit marker (matches pytest-asyncio in requirements).
testpaths = tests
asyncio_mode = auto
addopts = -q

