# FastAPI and server
fastapi==0.115.0
# Pinned explicitly: FastAPI 0.115 needs starlette<0.39, but the mcp package
# would otherwise pull a much newer Starlette and break the app. Keep in sync
# with FastAPI's supported range if FastAPI is upgraded.
starlette==0.38.6
uvicorn[standard]==0.30.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.52
alembic==1.13.3
psycopg2-binary==2.9.12  # PostgreSQL driver (Supabase)

# GitHub integration
PyGithub==2.1.1

# Markdown and YAML parsing
python-frontmatter==1.1.0
markdown==3.5.2
pyyaml==6.0.3

# LLM providers
anthropic==0.40.0
openai==1.51.0

# OAuth
authlib==1.3.0
requests==2.31.0

# Security and rate limiting
slowapi==0.1.9

# MCP server (exposes the vault to MCP clients like Claude Desktop/Code).
# Pinned: newer mcp pulls Starlette >=1.0 which conflicts with FastAPI 0.115's
# starlette<0.39 ceiling. mcp 1.2.1 works with the app's Starlette range.
mcp==1.2.1

# Utilities
python-multipart==0.0.6
aiofiles==23.2.1
psutil==5.9.8  # system metrics for /monitoring

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
